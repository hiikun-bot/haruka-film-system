-- 2026-08-22b マイゴール: タスクの担当メモ（複数名・非共有）
--
-- 背景:
--   タスク登録モーダルの「担当者」を自分固定からHFメンバー複数選択に拡張（ユーザー要望）。
--   ただしマイゴールは完全個人領域（ADR 032）のため、これは「誰と一緒にやるかの自分用メモ」。
--   選ばれたメンバー側には一切表示・通知されない（共有タスク化はしない。ユーザー確認済み 2026-08-22）。
--   users への FK は貼らない（配列型のため）。表示時に存在しない id は無視される。
--
-- 本番Supabaseへの適用が必要。

ALTER TABLE personal_tasks ADD COLUMN IF NOT EXISTS assignee_user_ids UUID[];
