-- ============================================================
-- クリエイティブのクライアント確認用URLを追加
-- ------------------------------------------------------------
-- 目的: ステータス「クライアント確認」進行時の通知に埋め込む
--   Drive シェアリンク等を保持するカラムを追加。
--   従来は notifications.js のテンプレートに "..." がハードコード
--   されており404の原因となっていた。
-- ============================================================
ALTER TABLE creatives
  ADD COLUMN IF NOT EXISTS client_review_url TEXT;

NOTIFY pgrst, 'reload schema';
