-- ============================================================
-- クリエイティブの素材フォルダURLを追加
-- ------------------------------------------------------------
-- 目的: 従来 memo 欄に「▶素材 https://drive.google.com/...」と
--   手書きで貼り付けて運用していた素材フォルダのリンクを、
--   専用カラムで保持できるようにする。
--   クリエイティブ追加モーダルに「素材フォルダURL（任意）」入力欄を
--   置くための受け皿。
-- 適用: Supabase SQL Editor に貼って Run。冪等。
-- バグ報告: #739526eb-1524-409b-84f3-d1103d0962c3
-- ============================================================
ALTER TABLE creatives
  ADD COLUMN IF NOT EXISTS material_folder_url TEXT;

NOTIFY pgrst, 'reload schema';
